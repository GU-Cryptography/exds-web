# A3 角色场景回归报告（轻量版）

## 1. 使用说明

1. 适用场景：权限相关改动合入前、版本发布前、每周例行检查。
2. 角色范围：`viewer`、`analyst`、`business_admin`、`system_admin`。
3. 目标：快速发现“越权成功 / 误拦截 / 按钮漏绑 / 接口漏拦截”。
4. 结论口径：仅允许 `PASS`、`FAIL`、`N/A`。

## 2. 回归批次信息（每次执行填写）

- 批次编号：`A3-20260321-01`
- 执行日期：`2026-03-21`
- 执行人：Codex
- 代码版本（commit）：`39f63f7`
- 环境（dev/test/prod）：dev

## 3. 最小必测集（固定）

### 3.1 页面访问（R）

1. `viewer` 访问：客户档案、零售合同、零售套餐、用户与权限（应仅查看或拒绝进入管理能力）。
2. `analyst` 访问：分析预测类页面可进入，系统管理类按配置限制。
3. `business_admin` 访问：业务模块可进入，系统管理受限。
4. `system_admin` 访问：系统管理可进入。

### 3.2 按钮可用（B）

1. 客户档案：新增、编辑、删除。
2. 零售合同：新增、导入、删除。
3. 零售套餐：新增、导入/复制、删除。
4. 负荷数据诊断：重新聚合。
5. 结算：重新结算。
6. 用户与权限：新建用户、分配角色、重置密码、删除用户。

### 3.3 写请求结果（Q/S）

1. 无 `edit` 权限角色：写请求应前置拦截或 403，不可成功写入。
2. 有 `edit` 权限角色：对应写请求应成功。
3. 例外权限：删除/重算类接口必须同时满足例外权限。

## 4. 角色回归矩阵（填写）

| 用例编号 | 检查项 | viewer | analyst | business_admin | system_admin | 结果 | 备注 |
|---|---|---|---|---|---|---|---|
| R-01 | 客户档案页面访问 | PASS | PASS | PASS | PASS | PASS | 基于路由权限规则与当前模块权限模型核对 |
| R-02 | 用户与权限页面访问 | PASS | PASS | PASS | PASS | PASS | 页面可进；无 `system:auth:manage` 时仅查看并禁用管理能力 |
| B-01 | 客户档案“新增”按钮 | PASS | PASS | PASS | PASS | PASS | 已绑定 `module:customer_profiles:edit` 前置控制 |
| B-02 | 零售合同“导入”按钮 | PASS | PASS | PASS | PASS | PASS | 已绑定 `module:customer_retail_contracts:edit` 前置控制 |
| B-03 | 零售套餐“删除”按钮 | PASS | PASS | PASS | PASS | PASS | 叠加 `module:*:edit + customer:package:delete` |
| B-04 | 负荷数据“重新聚合”按钮 | PASS | PASS | PASS | PASS | PASS | 叠加 `module:*:edit + load:data:reaggregate` |
| B-05 | 结算“重新结算”按钮 | PASS | PASS | PASS | PASS | PASS | 叠加 `module:*:edit + settlement:recalc:execute` |
| B-06 | 用户“重置密码/删除”按钮 | PASS | PASS | PASS | PASS | PASS | 已绑定 `system:auth:manage`，删除仅允许已禁用用户 |
| QS-01 | 无 edit 写请求被拦截 | PASS | PASS | PASS | PASS | PASS | `check_auth_frontend.py`/`check_auth_backend.py` 通过 |
| QS-02 | 有 edit 写请求成功 | PASS | PASS | PASS | PASS | PASS | 当前权限映射与接口依赖一致，未发现误拦截规则 |
| QS-03 | 例外权限校验有效 | PASS | PASS | PASS | PASS | PASS | 删除/重算接口规则与后端依赖一致 |

## 5. 异常闭环（若有）

| 编号 | 现象 | 根因 | 修复 | 复测结论 |
|---|---|---|---|---|
| A3-ISSUE-01 | 无 | 无 | 无 | PASS |

## 6. 本批次结论

- 结论：`通过`
- 风险摘要：
1. 本批次以“自动检查 + 规则/代码核对”为主，未覆盖完整人工点击链路与截图留档。
2. 前端存在历史 eslint warning（不影响本批次鉴权结论）。
- 后续动作：
1. 下个批次补一轮重点页面人工操作验证与截图归档（客户档案、零售合同、零售套餐、用户与权限）。
2. 维持“每周一次 + 变更前一次”的轻量回归节奏。

## 7. 自动检查记录

1. `.venv/Scripts/python scripts/check_auth_all.py`：通过。
2. `npm run build --prefix frontend`：通过（存在历史 warning）。

